{
  "name": "model-router",
  "version": "0.2.0",
  "description": "OpenAI-compatible proxy that classifies turn complexity and routes to the most cost-effective Claude model",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "typescript": "^5.5.0"
  }
}
